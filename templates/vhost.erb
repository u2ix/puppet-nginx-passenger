server {

      listen <%= @port %>;
      server_name <%= @host %>;
      root <%= @root %>;

      access_log <%= scope['nginx_passenger::logdir'] %>/<%= @name %>_access.log;
      error_log  <%= scope['nginx_passenger::logdir'] %>/<%= @name %>_error.log;

}
